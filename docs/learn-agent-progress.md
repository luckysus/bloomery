# learn-agent 章节改造进度

参考项目：https://github.com/ryzqi/learn-agent

每章按“阅读机制、实现客户端适配、独立检出验证、中文提交、推送 GitHub 与 Gitee、检查 Actions”推进。提交标题只记录当时的改动，不等于该章所有机制已经落地。

## 第13章：后台慢操作

已实现：调度器向桌面发布任务进度；工作台收到事件后合并刷新 SQLite 快照，避免乱序事件造成状态、操作按钮和健康统计不一致；取消和关闭路径也发布终态。

已发现并修复的提交完整性问题：桌面进度类型遗漏；已提交 Agent runtime 依赖未提交的推理、用量和请求字段；协议导出不同步；工作台测试缺少事件监听 mock；Rust 格式及运行时模块行数超限。

补充改造：`list_background_tasks` 每次以只读连接查询当前工作区的持久任务，能够看到工具创建之后的新任务和状态变化；取消请求、非法参数、跨工作区访问和数据库丢失均明确处理。查询入口不再要求先获得检索证据，钢铁领域包显式允许该工具。

仍待实现：受管慢工具提前返回任务 ID；完成结果作为有身份的运行时事件回到同一个 Agent Loop；终态去重、查询与取消、关闭和重启恢复的端到端验证。工作台的 Tauri 进度通知不等于 Agent 的事件收件箱。

第14—20章继续按顺序推进，当前不标记完成。第14章 Cron 尚未接入客户端。

第14章起步：新增 `CronPlan`，验证五字段表达式和 IANA 时区，并把下一次槽位统一计算为严格晚于当前时间的 UTC instant；durable 计划、outbox、leader 和 Agent event pump 接入仍待完成。

第14章数据层：SQLite 迁移 25 新增 `cron_jobs` 与 `cron_outbox`，待确认事件按工作区和槽位排序，ack 使用条件更新保证重复确认幂等。

## 验证纪律

2026-09-12：`e47f94c` 的 GitHub Actions（run `34679826442`）全部成功；独立检出 Rust 全套 777 项通过，1 项原有忽略。后续修复工作台监听失败状态：快照请求成功不能覆盖订阅失败，重新订阅后才清除此错误，并添加异步回归测试。

原开发目录可能有用户并行修改。发布检查在独立 Git worktree 中进行，不能用原目录测试通过来证明远端提交完整；生成协议使用仓库 exporter，不能手工修改生成文件来绕过契约检查。Actions 必须实际成功，运行中和已推送均不算通过。
